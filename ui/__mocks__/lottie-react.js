import React from 'react';

const Lottie = ({ animationData, loop, autoplay, style }) => (
  <div data-testid="lottie-mock" style={style}>
    Mock Lottie Animation
  </div>
);

export default Lottie;
